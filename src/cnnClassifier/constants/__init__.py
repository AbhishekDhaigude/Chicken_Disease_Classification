from pathlib import Path
#We are updating the constants file to include paths for configuration and parameters files.
#Also we will update this file 0nly once in the project.
#So keep it in the constants folder.

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")